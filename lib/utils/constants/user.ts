import { TUserBadgeCategory } from "@/app/types/features/user";

/**
 * Categories for user badges
 * They are temporary until we fetch them from the server
 */
const userBadgeCategories: TUserBadgeCategory[] = [
    {
        type: 'role',

    }
]